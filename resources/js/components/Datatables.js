import React from 'react';
import ReactDOM from 'react-dom';

const Datatables = () => (
  <h1>TEST</h1>
);

export default Datatables;

ReactDOM.render(<Datatables />, document.getElementById('datatables'));